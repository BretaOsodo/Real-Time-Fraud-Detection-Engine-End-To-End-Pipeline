[project]
name = "real-time-fraud-detection-engine-end-to-end-pipeline"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "confluent-kafka>=2.14.2",
    "faker>=40.21.0",
    "numpy>=2.4.6",
    "pytest>=9.0.3",
]
